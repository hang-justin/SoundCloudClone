

const PlayButton = () => {

  // needs to display pause button and needs pause functionality if track being played is the song

  


  return (
    <button onClick={handlePlay}>
      Play Button Image
    </button>
  )
}

export default PlayButton;
