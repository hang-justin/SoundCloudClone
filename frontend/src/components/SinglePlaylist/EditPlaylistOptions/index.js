

import './EditPlaylistOptions.css'

const EditPlaylistOptions = ({ playlist }) => {

    return (
        <div id='edit-playlist-options'>
            Edit playlist button and delete playlist button
        </div>
    )
};

export default EditPlaylistOptions;
